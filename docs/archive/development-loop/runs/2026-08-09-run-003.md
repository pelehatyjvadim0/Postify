# Запуск 003 — Wave 4

## Переход

`S_n`: `96bde41`, нет Telegram delivery. `S_n+1`: три Telegram slots, сохранённый `message_id`, идемпотентный повтор и безопасный cleanup. Code head `298f628`; live-проверенный и слитый в `main` head `17bb257`. Слияние выполнено fast-forward без pull и push.

## Доказательства

На feature и после merge в `main` получен одинаковый gate: non-integration `390 passed`; PostgreSQL integration `59 passed`; migration downgrade/re-upgrade `2 passed`; wheel+installer `29 passed`; compileall, `uv lock`, diff и scoped Ruff GREEN. Full repo Ruff: 10 pre-existing test-only F401 вне scope.

Live Telegram preflight на package `1` подтвердил:

- бот имеет `can_post_messages=true`;
- первый `publish-once` завершился с кодом `0`, `message_id=6`;
- package и delivery получили `published`, `confirmed_at` установлен;
- сохранена ровно одна attempt `published` без error code;
- delivery/package получили `media_deleted_at`, локальный файл удалён;
- повторный запуск завершился «Слот пуст», `message_id` и attempt count не изменились.

Token не выводился. Для preflight Telegram credentials, timeout и schedules были переданы только в process environment. Перед эксплуатацией локальный `.env` ещё нужно заполнить несекретными `TELEGRAM_CHAT_ID`, `TELEGRAM_TIMEOUT_SECONDS` и тремя `TELEGRAM_ON_CALENDAR_*`; token должен поступать через безопасную secret-конфигурацию.

## Профиль нагрузки

Поведения: queue, adapter, CLI, systemd, migrations, packaging. Границы: PostgreSQL, HTTP, filesystem, systemd, wheel. Компоненты: domain/application/adapter/repository/config/deploy. Последовательная глубина: migration → queue → adapter → composition → lifecycle. Фактически 40 файлов, diff до финального журнала `+2627/-56`, 18 feature-коммитов. Время/tokens/cost: не измерено.

## Хронология и возвраты

Sol High подготовил RED; Terra Medium реализовал и исправлял; fresh Terra High провёл test/mutation и code review. Возвраты: test design (tautology/lock), implementer (composition/cleanup), architecture/config (Telegram isolation/FIFO), systemd/integration (schedules/graceful stop), tooling (F401). Root провёл live Telegram preflight и доказал публикацию, cleanup и идемпотентный повтор. После fast-forward полный gate повторён на `main`; feature-ветка и worktree удалены, `.venv`, build, egg-info и Python/test caches перемещены в корзину. Bottleneck: внешние boundary contracts и их доказательства.

## Оценка и решения

Мутации покрывают SKIP LOCKED, cleanup, confirmation/delete, multipart, FIFO, dotenv parser и timeout publish service. Scoped, live и merged-main gates GREEN. Статус: `принят`; `main` чист, служебной ветки и worktree Wave 4 нет. Перед эксплуатацией остаётся только перенести уже проверенные несекретные Telegram-параметры в локальный `.env`.
