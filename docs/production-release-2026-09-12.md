# Релиз OpenRouter — 12 сентября 2026

Исправлены дефекты, найденные в [первом E2E-прогоне](e2e-2026-09-12.md).

- Агент, судья, vision и embeddings используют сохранённый ключ OpenRouter. Codex не входит в рабочий контур этого релиза.
- Числа и адреса проверяются целиком: 30 не подтверждается источником с 130; знак процента нельзя добавить к числу без основания.
- Судья допускает смысловую перефразировку, но обязан вернуть подтверждение и цитату из исходной темы. Несуществующая или пустая цитата не проходит.
- API одобрения требует успешный актуальный отчёт. При правке текста/изображения прежний отчёт сбрасывается, чтобы одобрение не использовало результат для старого текста.

Проверки: 575 unit/integration, включая 3 packaging-теста в чистой копии; 5 Playwright-сценариев desktop/mobile; сборка и typecheck. Live OpenRouter: 10 passed — создание и переписывание, восемь негативных guardrails и достоверная перефразировка. Результаты разных запусков суммированы только для непересекающихся тестов; полного единого запуска с внешними сервисами нет.

Старый сервер имел схему `20260905_19`, новый — `20260911_06`; прямой цепочки миграций между ними нет. Развёртывание использует отдельный Compose project `autoposttg-v2` и новые volumes. Старые база, файлы и резервная копия сохранены. Переносится проект и канал подтверждённого владельца `@sorrow9`; старая история остаётся в прежней базе.

В Linux-контуре `compose.production.yaml` приложение использует сеть хоста для доступа к локальной очереди общего бота на `127.0.0.1:8080`. PostgreSQL доступна только на `127.0.0.1:55433`. OpenRouter задан явно, ключ шифрования обязателен. Локальный backend с тем же auth relay остановлен: у очереди один потребитель. Dev UI может подключаться к серверу через SSH-туннель.

Проверка настоящего расписания выполняется `scripts/verify_production_e2e.py` внутри контейнера. Она создаёт отдельные аккаунт и проект, использует реальные OpenRouter/Telegram и системные часы, ждёт генерацию и публикацию от фонового планировщика, затем удаляет только собственное сообщение и тестовые данные. Живой Telegram-вход оператора проверяется отдельно; тестовая identity внутри smoke-скрипта не является доказательством живого входа.

Команды нового контура:

```bash
cd /opt/autoposttg-next
docker compose -p autoposttg-v2 -f compose.yaml -f compose.production.yaml up -d --no-build
docker compose -p autoposttg-v2 -f compose.yaml -f compose.production.yaml ps
```

Откат сервиса: остановить только `app` нового контура, затем запустить `app` прежнего `/opt/autoposttg`. Volumes не удалять. Новые данные остаются в базе v2 для последующего возврата; старый сервис их не читает.

Финальная проверка после переключения прошла на основном серверном порту 8000: OpenRouter → все слои проверки → автоодобрение → публикация фоновым планировщиком. Сообщение **15** опубликовано один раз и удалено после проверки; тестовые проект и аккаунт удалены. До переключения тот же сценарий прошёл с сообщением **14**, также удалённым. [Машинный отчёт продакшена](e2e/2026-09-12-fixed/production-e2e-result.json).

Живой вход оператора `@sorrow9` подтверждён; его сеанс сохранился после переключения. Проект `AutoPostTG` (id3) и прежний канал доступны владельцу, статус канала `ok`. Публичный UI проверен Playwright на desktop/mobile. Последняя проверка: приложение running, 0 перезапусков, 0 активных операций и 0 оставшихся тестовых аккаунтов.

Рабочий адрес: **http://148.251.35.72:8000/**. Локальный **http://127.0.0.1:5174/** подключён к этому продакшену через SSH. Режим пользовательского проекта — review; медиапул нового проекта пуст, для новых постов нужно загрузить изображения. Старый медиакаталог также был пуст.
