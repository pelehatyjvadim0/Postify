# Запуск 004 — Wave 5

## Переход

`Sₙ`: `main@efe959c` хранит кандидатов, решения, content attempts, packages и
Telegram-delivery, но `status` показывает лишь доступность БД, candidates и
systemd. Результат запуска, история delivery и объяснение дефицита не имеют
одного долговечного операторского снимка.

`Sₙ₊₁`: code head `e012da1` добавляет durable journal `operation_runs` для `run-once` и
`publish-once` и read-only `postify status`: состояния сущностей, последние
attempts и runs, покрытие плана из трёх публикаций, причины дефицита и safe
operational signals. Live-проверенный и слитый в `main` evidence head —
`9bc779d`; слияние выполнено fast-forward без pull и push.

## Доказательства

Исходный baseline — `390` non-integration / `59` integration. RED-коммиты
`a16571e`, `056ad73`: `51` unit RED, `5` CLI RED с `23` legacy pass, `9` PG RED
с `7` legacy pass, wheel `1` RED; production diff пуст. Финальный controller
gate: `468` non-integration / `69` integration; migrations `10`; wheel/CLI `8`;
compileall, scoped Ruff, lock, diff и CLI help — GREEN.

После fast-forward на `main` root повторил полный gate: `468` non-integration,
`69` integration, targeted migration `3`, CLI/wheel `7`, compileall, scoped
Ruff, lock, diff и CLI help — GREEN.

Live DB: до upgrade revision `20260809_04`, package `1` published, delivery
published, `message_id=6`, attempts `1`; upgrade до `05`. Реальный
`publish-once` дал `Слот пуст`, сохранил данные delivery и создал один
`publish_once/succeeded/empty`. Telegram adapter не вызывался: empty claim
queue. Два `status` оставили идентичные all-table fingerprints; status не пишет.
Итог: candidates `9/9 selected`, attempts `analyzed_not_selected=1`,
`packaged=1`, package/delivery published, attempt `1`, message `6`, план
`3/1/0/2`, причины `package_limit_reached`, `analysis_limit_reached`,
`processing_backlog`, проблемы `import_timer_inactive`,
`publish_timer_inactive`, `operation_runs=1`.

Изолированный migration-тест доказал `04 → 05 → 04 → 05` с Wave-4 fixture.
Destructive downgrade live DB не выполнялся. DSN, secrets и fake Telegram
values не выводились; безопасные временные метки в исходных результатах не
зафиксированы.

## Профиль нагрузки

Поведение: durable operation lifecycle, immutable/read-only snapshot, entity
states/history, deficit taxonomy, DB/systemd signals, migration и wheel.
Границы: PostgreSQL, CLI, systemd probes, packaging и live smoke DB.
Фактический diff: `29` файлов, `+4080/-80`. Время, tokens и cost: не измерено.
Агентные прогоны: Sol High — RED; Terra Medium — GREEN и исправления; fresh
Terra High — staged независимое review.

## Хронология, возвраты и потери

1. Stage 1: `0C/2I` → `f460cc6` → `0C/0I/0M`; класс потерь — **тестовый дизайн**:
   отсутствовали load-bearing factory tests для daily target и publish journal.
2. Stage 2: `0C/5I` → `df87295`/`691d300` → `0C/2I` → `7b39fcc` → `0C/0I/0M`;
   класс потерь — **reviewer**: выявлены renderer/report safety и fixed-group/code
   contract gaps до приёмки.
3. Live: `1 Important` → `e012da1` → `0C/0I/0M`; класс потерь — **интеграция**:
   реальное systemd-состояние выявило inactive timers, которые тестовая policy
   ошибочно считала healthy.

Не зафиксированы потери классов: спецификация, декомпозиция, исполнитель,
инфраструктура, инструментарий, нарушение границ задачи. Bottleneck: доказать
согласованность долговечного DB snapshot и safe live boundary без Telegram
вызова.

## Решение

Статус: **принят**. После merged-main GREEN feature-ветка и worktree Wave 5
удалены; `.venv`, build, egg-info и Python/test caches перемещены в корзину.
`main` чист, push не выполнялся. Следующая продуктовая работа должна начинаться
от принятого `main`, не расширяя Wave 5 в UI или конфигурацию Wave 6.
