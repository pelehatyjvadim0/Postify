# Запуск 006: Wave 6 — рабочий UI и настройки

Дата: 2026-08-12.

Актуальная граница 2026-08-17: продукт `497746a`, evidence-контракт
`e6af284`, детерминированная трасса `quality/07_*`; Wave 6 принята. Старые
этапы и числа ниже сохранены как хронология, а итоговая секция в конце является
управляющей.

## Переход

`Sₙ`: `feature/ui-v1-html-mockup@8423107` содержит семь UI-разделов,
backend settings и PostgreSQL scheduler, но не имеет installed-wheel e2e,
полных операторских инструкций и финальной seeded-приёмки.

`Sₙ₊₁`: текущий коммит Task 11 доказывает работу wheel вне
checkout, создаёт проект при первом UI-запуске, раздаёт `/static`,
описывает install/migrate/secret/scheduler/backup/rollback и закрывает
две populated-API регрессии и одну full-suite isolation regression.

## Доказательства

- RED nodes: `W6-R1`–`W6-R11`;
- full PostgreSQL 16: `707 passed in 61.70s`;
- installed wheel: build, inspect, fresh venv install, packaged migrate, cwd вне
  checkout, HTTP `200/200/200`, bootstrap ID `1`, clean `SIGINT`, `1 passed`;
- compileall, scoped/full Ruff, lock, diff-check: GREEN;
- visual: 7 desktop `1440×1000`, 7 mobile `360×800`, loading/error,
  settings forms, logo/grid, no overflow;
- screenshots: `tmp/task11-visual/` (Git ignored).

## Профиль нагрузки

- изменение поведения: installed UI/bootstrap/static route и safe DTO
  serialization;
- внешние границы: PostgreSQL 16, wheel/pip/venv, localhost HTTP,
  Chrome/Playwright, process signals;
- unknowns до старта: полнота wheel, first-run bootstrap, seeded UI;
- время, токены и стоимость: не измерено.

## Хронология и возвраты

1. Full baseline вернул `16 failed, 688 passed`: session Playwright
   оставлял running loop для поздних `asyncio.run`. Класс потери:
   **интеграция**. Исправлен lifetime fixture.
2. Wheel e2e последовательно нашёл `404` для `/static/styles.css`
   и `404` для bootstrap. Класс: **интеграция**. Добавлен mount и
   существующий idempotent bootstrap action в composition root.
3. Seeded acceptance нашла materials/packages `503`. Класс:
   **тестовый дизайн**: пустая component fixture не доходила до
   `mappingproxy` и nullable filter. Добавлены два RED component tests.
4. Первый visual capture имел Playwright `CancelledError`, а reused-page
   contact sheet — промежуточные route states. Класс: **инструментарий**.
   Request завершается до close, каждый screen снимается в новой Page;
   финальный capture чист.
5. Full Ruff нашёл `11` F401. Класс: **интеграция**. Удалены
   два unused import; dynamic `locals()` test namespace заменён явным без
   изменения test behavior.
6. Important review нашёл caller-environment leak в wheel e2e. Класс:
   **тестовый дизайн**. Hostile ambient Fernet/Telegram/HN/timezone
   создал channel/route (`1 failed in 3.26s`). Build/install/runtime env
   заменён OS allowlist и явными тестовыми values; GREEN `1 passed in 3.32s`.

Bottleneck: доказать одновременно hermetic wheel и populated UI на
реальной PostgreSQL-границе.

## Решение

Статус: **готов к решению root**. Это не глобальная приёмка:
push/merge не выполнялись, worktree не удалялся. Временные
PostgreSQL schemas и одноразовый PostgreSQL 16 container удалены после
финального gate Task 11.

## Remediation Run-006 — 2026-08-17

Исторический статус выше был отменён final re-review на `5ec40be`. Новый переход:

- `Sₙ`: `5ec40be`, NOT APPROVED — wrong-route retry/schedule, crash-orphaned
  accepted work, неполный Journal/attempt UI и недолговечные visual claims.
- `Sₙ₊₁`: `497746a` — route-safe delivery, recoverable queued/leased jobs,
  complete operational Journal/attempt fields, executable environment/backup
  commands и durable 16-PNG manifest.

Свежие gates: full `741 passed`; focused backend `37 passed`; browser
`77 passed`; installed wheel `1 passed`; Ruff, compileall, lock, четыре
`node --check` и оба diff-check exit `0`. PostgreSQL boundary — отдельный
PostgreSQL 16.14 на loopback; рабочий DSN/секреты не записаны. Первый
специализированный browser gate вернул setup errors только потому, что явно
указанный `/dev/shm` TMPDIR ещё не существовал; после `mkdir -p` команда
повторена без test failures.

Visual command:

```sh
uv run python docs/development-loop/evidence/artifacts/2026-08-12-wave-6/capture.py
```

Результат: 7 desktop + 7 mobile ready PNG, desktop loading, mobile error,
валидный `manifest.json`, `0` overflow entries. Bottleneck remediation —
связать atomic database acceptance с фактическим worker lifecycle; решено
отдельной `scheduled_jobs` queue и lease/ack recovery.

Статус: **Wave 6 APPROVED**. Merge, push, worktree deletion и cache cleanup не
выполнялись.

## Итоговая синхронизация evidence — 2026-08-17

Продуктовая граница остаётся `497746a`; после неё `143ce0a` добавил durable
evidence, а `e6af284` исправил только capture/manifest-контракт и три PNG.
Именно дерево `e6af284` прошло независимую трассу из 14 gates:

- PostgreSQL 16 full suite: `741 passed in 51.10s`;
- focused backend: `12 passed in 2.03s`;
- browser: `77 passed in 28.51s`;
- installed wheel: `1 passed in 3.08s`;
- Ruff: `All checks passed!`; compileall, 4 JavaScript syntax checks и 2
  diff-check — exit `0`; lock — `Resolved 43 packages in 0.87ms`;
- visual validator: `16 PNG captures`, manifest и hashes валидны, у всех
  записей `horizontal_overflow=false`.

PostgreSQL 16 был фактически подготовлен и запущен без пароля как отдельный
user-owned loopback test cluster:

```sh
/usr/lib/postgresql/16/bin/initdb -D /dev/shm/postify-sssf-pg16 --username=postify_gate --auth=trust --encoding=UTF8 --no-locale
mkdir -p /dev/shm/postify-sssf-pg16-socket
/usr/lib/postgresql/16/bin/pg_ctl -D /dev/shm/postify-sssf-pg16 -l /dev/shm/postify-sssf-pg16.log -o "-h 127.0.0.1 -p 55434 -k /dev/shm/postify-sssf-pg16-socket" start
```

Первая попытка `pg_ctl` без `-k` не стартовала; приведённая команда с отдельным
socket-каталогом завершилась успешно. `pg_isready` подтвердил loopback
connection, сервер сообщил PostgreSQL `16.14`. Пароли и рабочие DSN не
фиксировались.

Durable visual matrix находится в
`docs/development-loop/evidence/artifacts/2026-08-12-wave-6/manifest.json`:
7 desktop и 7 mobile ready PNG, desktop loading и mobile error. Manifest
записывает воспроизводимую capture-команду:

```sh
uv run python docs/development-loop/evidence/artifacts/2026-08-12-wave-6/capture.py
```

Статус: **Wave 6 APPROVED на evidence boundary `e6af284`**. Независимая запись
приёмки: `.superpowers/sdd/2026-08-12-ui-backend-settings/final-rereview-3.md`.
Merge, push, проверка на `main`, удаление worktree и cleanup не выполнялись.
