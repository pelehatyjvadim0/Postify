# Контур разработки

Версия процесса: `0.4`

## Подтверждённые проблемы

| Проблема | Доказательство | Влияние | Действие | Статус |
| --- | --- | --- | --- | --- |
| Нет сопоставимых eval по нескольким вейвам | Wave 1 измерена задним числом и не содержит времени, токенов и стоимости | Нельзя определить безопасный размер вейва | Собирать полный eval со следующего запуска | Открыта |
| Systemd не проверен на реальной машине | Проверены имитация границы и сквозные shell-тесты без `systemctl` и журнала | Wave 1 нельзя считать проверенной в рабочем окружении | Провести живую проверку перед эксплуатацией | Открыта |
| Первичные тесты пропускали неверную логику | Возвраты по HN HTTP, конкурентности PostgreSQL и откату | Ложный GREEN увеличивает число проходов | Sol 5.6 High пишет RED-тесты, трассировку и карту мутаций | Учтена процессом |
| Подменяемая граница не доказывает совместимость реального внешнего протокола | Wave 3: Wikimedia потребовала request identity/namespace, Codex отклонил неподдерживаемую strict schema | Hermetic GREEN может не дойти до полезного результата вживую | После review выполнять минимальный разрешённый live preflight и возвращать находки в TDD | Учтена процессом |

Очевидная проблема блокирует следующий вейв, только если угрожает его переходу или качеству доказательства. Основание блокировки фиксируется явно.

## Буфер наблюдений

| Наблюдение | Основание | Что измерять | Порог решения |
| --- | --- | --- | --- |
| Внешние границы могут быть главным источником возвратов | PostgreSQL, systemd и упаковка заняли большую часть цикла исправлений Wave 1 | Границы, возвраты и время по каждому вейву | Не менее 3 сопоставимых вейвов |
| Документационные задачи могут хуже исполняться отдельными агентами | Два зависания и одно нарушение scope на README | Запуски, зависания и переделки doc-задач | Повторение ещё в 2 запусках |
| Большой интеграционный скоуп может скрывать слабые тесты | Несколько разных границ внутри Wave 1 | Эффективность мутаций и доля переделок по размеру скоупа | Не менее 3 вейвов разных размеров |

Наблюдение не меняет процесс автоматически. Оно переносится в подтверждённые проблемы после достижения порога или снимается при опровержении.

## Определение вейва

> Вейв — не состояние системы, а управляемый переход из подтверждённого состояния `Sₙ` в новое подтверждённое состояние `Sₙ₊₁`.

> Вейв — атомарный проверяемый переход между двумя подтверждёнными состояниями системы.

```text
Sₙ + изменения вейва → Sₙ₊₁ + доказательства
```

Вейв обязан:

- иметь одну логическую цель;
- создавать наблюдаемое изменение;
- иметь независимое доказательство;
- быть отдельно принимаемым или отклоняемым;
- не требовать будущего вейва для доказательства результата;
- помещаться в измеренную ёмкость цикла.

Результатом может быть пользовательская возможность, техническая возможность, интеграция, проверяемый фундамент или снятая неизвестность.

## Состояния

Git хранит техническое состояние. Документ объясняет его смысл.

Для каждого вейва фиксируются:

- `Sₙ`: исходные свойства и базовый коммит;
- `Sₙ₊₁`: принятые свойства и итоговый коммит;
- доказательства перехода;
- статус: `принят`, `отклонён`, `заблокирован` или `откачен`.

## Процесс

1. Получить вейвы.
2. Проверить атомарность и доказуемость каждого перехода.
3. Оценить нагрузку и зависимости.
4. Определить последовательность и безопасный параллелизм.
5. Передать Sol 5.6 High написание RED-тестов без рабочего кода.
6. Sol фиксирует трассировку требований, причину каждого RED и карту неверных реализаций.
7. Sol проверяет, что RED вызван отсутствующим поведением, а не окружением.
8. Передать тестовый пакет исполнителю Terra 5.6 Medium.
9. Реализовать минимальный код до GREEN.
10. Передать свежей Terra 5.6 High независимую проверку тестов и мутаций.
11. Вернуть в цикл исправлений при слабом доказательстве.
12. После прохождения тестового барьера выполнить ревью кода той же Terra.
13. Исправлять до отсутствия Critical и Important.
14. Проверить переход `Sₙ → Sₙ₊₁` вживую настолько, насколько позволяет граница.
15. Слить принятый вейв в `main`.
16. Повторить полный тестовый gate на `main`.
17. Только после GREEN удалить завершённые ветки и worktree вейва.
18. Удалить генерируемые артефакты и кэши.
19. Подтвердить чистый `git status` на `main`.
20. Сформировать eval и журнал запуска.
21. Обновить проблемы, наблюдения и оценку ёмкости цикла.
22. Скорректировать скоуп следующих вейвов.

Sol проектирует доказательство. Terra Medium реализует его. Свежая Terra High независимо проверяет тесты, затем код.

## Завершение вейва

Вейв завершён только при выполнении всей цепочки:

```text
accepted → merge main → полный GREEN → удаление ветки/worktree → очистка → чистый Git
```

Правила:

- незавершённые и параллельно активные ветки не удаляются;
- при сбое тестов на `main` ветка сохраняется, вейв возвращается в цикл исправлений;
- удаляются `.venv`, `build`, `dist`, `*.egg-info`, тестовые и Python-кэши;
- `.env`, пользовательские данные и несвязанные файлы не удаляются;
- перед следующим вейвом `main` обязан иметь пустой `git status --porcelain`.

## Схема LOOP

![[workflow.excalidraw|100%]]

Редактируемый источник: `workflow.excalidraw.md`. Схема описывает механизм текущей версии без данных конкретных вейвов. Версия схемы обязана совпадать с версией этого файла.

## Eval вейва

### Нагрузка

- изменения поведения;
- внешние границы;
- неизвестности до старта;
- затронутые компоненты;
- глубина последовательных зависимостей;
- доступный параллелизм;
- ожидаемые и фактические файлы.

### Работа цикла

- полное время;
- запуски агентов;
- итерации реализации;
- возвраты;
- reviewer-проходы;
- токены и стоимость, если доступны;
- размер переделки.

Неизвестные значения отмечаются `не измерено`. Денежные оценки не выдумываются.

### Качество

- причина исходного RED;
- полнота трассировки требований Sol;
- доля критериев с тестом и мутацией;
- результат первого GREEN и ревью Terra;
- количество и типы тестов;
- мутации до и после реализации, доля RED;
- Critical, Important и Minor;
- дефекты после принятия;
- живая проверка результата;
- проверка rollback.
- полный GREEN после merge в `main`;
- чистота репозитория после завершения.

### Причины потерь

Используются фиксированные классы:

- спецификация;
- декомпозиция;
- исполнитель;
- тестовый дизайн;
- reviewer;
- интеграция;
- инфраструктура;
- инструментарий;
- нарушение границ задачи.

Каждый возврат содержит причину, класс, потери и исправление.

## Производные показатели

| Показатель | Смысл |
| --- | --- |
| Приёмка с первого прохода | Доля вейвов без возврата |
| Доля переделок | Доля повторно выполненной работы |
| Эффективность мутаций | Доля мутаций, давших RED |
| Покрытие трассировки RED | Доля критериев с тестом, причиной RED и мутацией |
| Частота возвратов | Число возвратов на вейв |
| Время принятого перехода | Время до доказанного `Sₙ₊₁` |
| Стоимость принятого перехода | Стоимость доказанного перехода |
| Утечка дефектов | Дефекты, найденные после принятия |
| Доля живых доказательств | Доля результата, проверенная на реальной границе |
| Чистое завершение | Доля вейвов, завершённых на чистом `main` без служебных веток и артефактов |

Первые 3–5 вейвов — калибровочные. До накопления данных единый рейтинг не рассчитывается.

## Ёмкость цикла

Размер вейва определяется не строками кода, а сочетанием:

```text
поведение + внешние границы + неизвестности + связность
```

Ёмкость подтверждается только повторяемыми результатами. Порог перегрузки найден, если с ростом профиля нагрузки устойчиво растут возвраты, время, стоимость или дефекты.

После калибровки здесь фиксируется рабочее ограничение с доказательствами по завершённым вейвам.

Текущая ёмкость: `не определена`.

## Сводные показатели

| Метрика | Значение |
| --- | --- |
| Измеренные вейвы | 3: Wave 1 — неполный ретроспективный eval; Wave 2 и Wave 3 — полные eval |
| Приёмка с первого прохода | Wave 2: нет; Wave 3: нет |
| Медианное время | Не измерено |
| Медианная стоимость | Не измерено |
| Эффективность мутаций | Wave 1: 14/14; Wave 2: 14/14 и 6/6; Wave 3: 12/12 основных gates |
| Покрытие трассировки RED | Wave 2: 14/14; Wave 3: 12/12 design mutation gates |
| Частота возвратов | Wave 2: 2; Wave 3: 10; Wave 1 не измерена единообразно |
| Утечка дефектов | 0 известных |
| Доля живых доказательств | Wave 2: domain/application/PostgreSQL; Wave 3: HTTP/Wikimedia/PostgreSQL/Codex; доля не вычислялась |
| Чистое завершение | 3/3 |

До пяти вейвов показываются значения и диапазон. Затем добавляются медиана и тенденция. Среднее без распределения не используется.

## Журнал запусков

Один запуск может получить один или несколько вейвов. После завершения создаётся один файл:

```text
runs/YYYY-MM-DD-run-NNN.md
```

Для каждого вейва он содержит:

1. цель перехода;
2. `Sₙ` и `Sₙ₊₁`;
3. базовый и итоговый коммиты;
4. доказательства;
5. профиль нагрузки;
6. хронологию попыток;
7. причины возвратов;
8. bottleneck;
9. оценку цикла;
10. решения перед следующим запуском.

- [2026-08-02, запуск 001](runs/2026-08-02-run-001.md) — Wave 2: отбор и журнал решений.
- [2026-08-09, запуск 002](runs/2026-08-09-run-002.md) — Wave 3: контентные пакеты.
- [2026-08-12, запуск 006](runs/2026-08-12-run-006.md) — Wave 6: рабочий UI, настройки и installed-wheel gate.

## Обновление harness

Любое изменение этапов или правил требует одним коммитом:

1. повысить версию процесса;
2. обновить этот файл;
3. обновить Excalidraw-схему;
4. записать причину изменения;
5. указать eval, на котором основано решение.

## История процесса

| Версия | Причина | Основание |
| --- | --- | --- |
| 0.4 | Добавлен обязательный merge в `main` и очистка репозитория | Завершение Wave 1 |
| 0.3 | Убрана Luna; RED-пакет проектирует Sol 5.6 High | Риск регрессии сильных тестов слабой моделью |
| 0.2 | Добавлен отдельный контроль RED-тестов до реализации | Слабые первичные тесты Wave 1 |
| 0.1 | Создан контур мониторинга | Ретроспектива Wave 1 |
