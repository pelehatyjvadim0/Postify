# RED-доказательства Wave 4

Исходный hermetic baseline: `327 passed`. PostgreSQL RED выполнен на безопасной smoke DB; DSN не выводился.

## Трассировка

| Требование | Тест | Ожидаемый RED | Мутация |
| --- | --- | --- | --- |
| Точные domain-типы и `approved → published` | `test_delivery_types_preserve_the_complete_public_contract`, `test_approved_package_has_only_the_published_terminal_transition` | Нет `postify.domain.delivery`; нет `PackageStatus.PUBLISHED` | Расхождение status/type; пропуск transition |
| Empty, stale-first, один claim | `test_empty_slot_converts_stale_before_reserving_one_package` | Нет application action | Skip stale; reserve two |
| Confirmation до delete | `test_confirmation_is_committed_before_media_delete`, `test_failed_confirmation_never_deletes_media_or_reports_published` | Нет application action | Delete before commit; publish before confirmation |
| Typed retryable/failed/uncertain | `test_typed_publish_failure_is_persisted_without_confirmation_or_delete` | Нет application action | Wrong branch; terminal retry |
| Cleanup-first без Telegram | `test_pending_cleanup_is_completed_before_claim_without_telegram`, `test_failed_cleanup_remains_pending_without_telegram_or_failure_attempt` | Нет application action | Telegram during cleanup; cleanup как attempt |
| Точный `sendPhoto` multipart | `test_send_photo_uses_exact_endpoint_and_multipart_contract` | Нет Telegram adapter | Wrong endpoint/field/MIME/file bytes |
| Безопасная классификация API/file/timeout | `test_ok_false_is_classified_without_leaking_response_or_request`, `test_malformed_success_is_uncertain_and_never_accepted`, `test_file_error_before_request_is_retryable_and_makes_no_http_call`, `test_transport_timeout_after_request_is_uncertain` | Нет Telegram adapter | Leak token; accept malformed JSON; retry timeout |
| FIFO только approved, one-per-call | `test_reserve_next_claims_only_one_oldest_approved_package` | Pending PostgreSQL execution; production module отсутствует | Wrong filter; reverse FIFO; reserve two |
| Concurrent claim без дублей | `test_two_concurrent_claims_receive_different_packages` | Pending PostgreSQL execution | Remove `SKIP LOCKED` |
| Одна delivery на package | `test_delivery_has_one_row_per_package` | Pending PostgreSQL execution | Remove unique |
| Только retryable даёт attempt + 1 | `test_retryable_is_the_only_failure_claimed_again_with_next_attempt` | Pending PostgreSQL execution | Auto-retry terminal/uncertain; stale attempt number |
| Stale sending → uncertain attempt | `test_stale_sending_becomes_uncertain_with_one_attempt` | Pending PostgreSQL execution | Skip conversion; omit attempt |
| Атомарный confirm и rollback | `test_confirmation_atomically_persists_attempt_delivery_message_and_package`, `test_confirmation_sql_failure_rolls_back_every_write` | Pending PostgreSQL execution | Omit attempt/message ID; partial commit |
| Только positive integer confirmation | `test_confirmation_rejects_missing_or_nonpositive_integer_message_id` | Pending PostgreSQL execution | Confirm without message ID; accept bool/string |
| Cleanup marker отделён от confirm | `test_cleanup_marker_is_persisted_separately_from_confirmation` | Pending PostgreSQL execution | Mark deleted during confirm; omit marker |
| Named migration, downgrade/re-upgrade с сохранением Wave 3 | `test_telegram_delivery_migration_has_exact_named_schema`, `test_telegram_delivery_migration_downgrades_to_wave_three_and_upgrades_again` | Pending PostgreSQL execution; Alembic head Wave 4 отсутствует | Omit constraint/timezone; broken rollback |
| Шесть обязательных Telegram settings | `test_settings_exposes_all_six_telegram_values_without_plain_token`, `test_settings_requires_each_telegram_value`, `test_settings_requires_positive_telegram_timeout`, `test_settings_rejects_blank_telegram_calendar` | Нет Telegram-полей/валидации | Missing field; plain token; zero timeout; blank schedule |
| CLI валидирует до boundary и печатает каждый safe outcome | `test_publish_once_prints_each_safe_outcome`, `test_publish_once_validates_settings_before_opening_boundary`, `test_publish_once_normalizes_runtime_error_without_secrets` | Нет команды `publish-once` | Open boundary before validation; leak secret; wrong outcome |
| Отдельные publish units, три slots, verify-before-copy | `test_installer_creates_separate_publish_units_and_verifies_all_before_copy` | Нет publish service/timer | Hardcode schedules; share run-once unit; copy before verify |
| Rollback четырёх unit после любого copy/signal | `test_installer_rolls_back_all_four_units_after_each_partial_failure` | Installer обрабатывает только два unit | Partial rollback |
| Чистый wheel содержит delivery и Alembic head | `test_clean_wheel_contains_delivery_modules_and_new_alembic_head` | Новых модулей/head нет в wheel | Omit module/migration from wheel |

## Команды

```bash
uv run pytest tests/unit/domain/delivery/test_models.py -q
uv run pytest tests/unit/application/delivery/test_publish_content.py -q
uv run pytest tests/unit/adapters/telegram/test_bot_api.py -q
env -u DATABASE_URL TEST_DATABASE_URL="$TEST_DATABASE_URL" uv run pytest tests/integration/infrastructure/test_sqlalchemy_delivery.py tests/integration/test_migrations.py -q
uv run pytest tests/unit/config/test_settings.py -q
uv run pytest tests/e2e/test_cli_publish_once.py -q
uv run pytest tests/e2e/test_systemd_installer.py -q
uv run pytest tests/unit/infrastructure/test_delivery_wheel.py -q
```

Фактический RED: domain `4 failed`; application `11 failed`; adapter `10 failed`; PostgreSQL/migrations на безопасной smoke DB `15 failed, 5 passed`; config `12 failed, 62 passed`; CLI `9 failed`; systemd `5 failed, 13 passed`; wheel `1 failed`. DSN не выводился. Все production-import отсутствующих API выполняются внутри test body/helper.
