# Wave 5: доказательства RED, GREEN и live-приёмки

Дата: 2026-08-09. База перехода: `efe959c`; проверяемый feature head: `e012da1`.
Merge в `main`, gate на `main` и очистка выполняются root и ещё не сделаны.

## Исходный RED

RED-коммиты: `a16571e`, `056ad73`. Production diff был пустым.

| Контур | Фактический результат | Причина RED |
| --- | --- | --- |
| Domain/application unit | `51 failed` | Отсутствуют типы и actions наблюдаемости. |
| CLI | `5 failed`, `23 passed` legacy | Нет полного/частичного status-контракта и observed bootstrap. |
| PostgreSQL | `9 failed`, `7 passed` legacy | Нет repository, `operation_runs` и миграции `20260809_05`. |
| Clean wheel | `1 failed` | В колесе нет шести Wave-5 модулей и migration head. |

PostgreSQL RED выполнялся с `DATABASE_URL` удалённой из окружения и URL только в
`TEST_DATABASE_URL`; DSN и секреты не выводились. RED вызван отсутствующим
Wave-5 API, не окружением.

## Трассировка и мутации

RED-пакет покрывает: фиксированные safe codes и terminal-инварианты;
`running → succeeded|failed` без изменения результата или исходного исключения;
локальный день и дефицит `max(3 - published - ready, 0)`; восемь причин в
стабильном порядке; unknown persisted state; read-only snapshot; committed
journal и conditional terminal transition; ready predicate, newest-ten safe
history, `REPEATABLE READ`; named migration `04 → 05 → 04 → 05`; CLI
full/partial safe output; bootstrap и clean wheel.

Stage 1 начал с `0 Critical / 2 Important`: мутация factory `daily_target=3 → 2`
пережила проверку, а `open_publish_once` не имел прямого доказательства
`RecordedAction`. Исправление `f460cc6` добавило оба load-bearing теста.
Финал Stage 1: `0C/0I/0M`, все `9/9` load-bearing мутаций дали RED; исходная
мутация daily target зафиксирована как единственная пережившая до исправления.

Stage 2 начал с `0C/5I`: дублирование PostgreSQL runtime-row, потеря signal IDs,
неполные нулевые группы, устаревшая README-граница и произвольные outcome codes.
После `df87295` и `691d300` остались `0C/2I`: candidate/rejection zero taxonomy
и произвольные failure codes. `7b39fcc` дал `0C/0I/0M`. Live-проверка выявила ещё
один Important: inactive timers ошибочно считались healthy; `e012da1` добавил
сигналы `import_timer_inactive` и `publish_timer_inactive`. Scoped повторная
проверка: `0C/0I/0M`.

## Финальные gates

- Baseline: `390` non-integration / `59` integration.
- Финальный controller gate: `468` non-integration / `69` integration.
- Targeted migrations: `10` passed; wheel/CLI: `8` passed.
- `compileall`, Ruff для изменённых `src` и tests, `uv lock --check`,
  `git diff --check`, CLI help и status help: GREEN.

## Live acceptance на feature

На существующей smoke DB до upgrade подтверждены revision `20260809_04`,
package `1` со статусом `published`, delivery `published`, `message_id=6` и
ровно одна attempt. После upgrade до `05` реальный `publish-once` получил
только process-only fake Telegram values: вывод — `Слот пуст`; Telegram adapter
не вызывался, поскольку claim queue пуста. Создан один `operation_runs`:
`publish_once/succeeded/empty`; package, delivery, `message_id=6` и attempt
count `1` не изменились.

Два запуска `status` дали идентичные fingerprints всех таблиц: сама команда не
создала строк. Итоговый снимок: candidates `9`, selected `9`; content attempts
`analyzed_not_selected=1`, `packaged=1`; package `1` и delivery — `published`;
history — attempt `1`, message `6`; target `3`, published `1`, ready `0`,
deficit `2`; причины `package_limit_reached`, `analysis_limit_reached`,
`processing_backlog`; проблемы `import_timer_inactive`, `publish_timer_inactive`;
`operation_runs=1`. Временные метки не включены в переданные безопасные выводы;
секреты и DSN не фиксировались.

Изолированный migration-тест доказал `04 → 05 → 04 → 05` с сохранением Wave-4
fixture. Destructive downgrade на live DB не выполнялся.

## Граница статуса

Статус: **готов к решению root**. Live acceptance принята на feature, но Wave 5
не является глобально `принятой`: обязательны merge, полный gate на `main` и
cleanup root.
