# Wave 5: RED-доказательства наблюдаемости

Дата: 2026-08-09. Baseline до Wave 5: 390 hermetic / 59 PostgreSQL.
Этот пакет меняет только tests и evidence; production-изменения запрещены.

## Hermetic RED

```text
uv run pytest tests/unit/domain/observability tests/unit/application/observability -q
51 failed; 0 collection errors
Причина: нет postify.domain.observability и postify.application.observability.
```

Импорты отсутствующего API находятся внутри test/helper: `--collect-only`
собирает все 51 test. RED вызван отсутствующим Wave 5 API, а не
окружением.

## Trace и мутации

| Требование | Test ID | Точная причина RED | Мутация | Ошибочный результат |
|---|---|---|---|---|
| Фиксированные operation/status codes | `test_operation_enums_expose_only_supported_safe_codes` | Нет domain API | Добавлен arbitrary code | Журнал принимает неизвестную операцию |
| Terminal consistency и timezone-aware | `test_operation_run_summary_*`, `test_persisted_summaries_*` | Нет typed summary | Пустой outcome/failure, naive time | Невозможный run попадает в отчёт |
| `running → succeeded` до/после action | `test_recorded_action_preserves_result_and_saves_safe_outcome` | Нет RecordedAction | Start after action; success before action | Вызов не виден или успех записан до результата |
| Результ action не меняется | `test_callable_success_outcome_uses_result_without_replacing_it` | Нет RecordedAction | Return outcome вместо T | CLI теряет исходный result |
| Безопасный failure code | `test_action_failure_saves_only_fixed_code_and_reraises_original_exception` | Нет RecordedAction | Arbitrary exception stored | В БД попадают URL/token/path/text |
| Исходная ошибка не маскируется | `test_failure_journal_error_never_masks_original_exception` | Нет RecordedAction | Journal failure masks original | Оператор видит вторичную ошибку |
| Action не стартует без journal | `test_start_failure_prevents_underlying_action` | Нет RecordedAction | Ignore start failure | Ненаблюдаемое выполнение |
| Terminal failure не повторяет action | `test_success_journal_failure_does_not_repeat_underlying_action` | Нет RecordedAction | Retry whole action | Повторная Telegram-отправка |
| Локальный день | `test_repository_receives_exact_local_day_as_half_open_utc_interval` | Нет ShowOperationalStatus | UTC-day вместо local-day | Считаются чужие публикации |
| Дефицит `max(3-published-ready, 0)` | `test_deficit_uses_only_today_published_and_claimable_ready` | Нет ShowOperationalStatus | Wrong target; package status вместо confirmed delivery; failed/uncertain as ready | Ложное покрытие плана |
| Восемь deficit reasons | `test_each_deficit_cause_has_one_stable_safe_reason`, `test_status_returns_all_applicable_reasons_in_fixed_order` | Нет ShowOperationalStatus | Одна причина вместо всех восьми | Теряется одновременный backlog/limit/failure |
| Unknown state видим как critical | `test_unknown_persisted_status_is_a_critical_signal` | Нет ShowOperationalStatus | Unknown status silently ignored | Новое/битое состояние показано healthy |
| Read-only status | `test_healthy_snapshot_is_pure_and_has_no_problem_signals` | Нет ShowOperationalStatus | Status writes row/claim/retry/cleanup | Чтение меняет production state |

## CLI, PostgreSQL и wheel RED

```text
uv run pytest tests/e2e/test_cli_status.py tests/e2e/test_cli_run_once.py -q
5 failed, 23 passed
Новые RED: нет full/partial status contract и observed bootstrap; существующие start/stop/run-once сценарии GREEN.

env -u DATABASE_URL TEST_DATABASE_URL="$test_url" uv run pytest \
  tests/integration/infrastructure/test_sqlalchemy_observability.py \
  tests/integration/test_migrations.py -q
9 failed, 7 passed
Новые RED: нет observability models/repositories, operation_runs и revision 20260809_05;
существующие migration cases GREEN.

uv run pytest tests/unit/infrastructure/test_observability_wheel.py -q
1 failed: clean wheel не содержит шесть Wave 5 modules/head.
```

PostgreSQL запущен через `../../.env.smoke-real`: `DATABASE_URL` удалён из
process environment, в pytest передан только `TEST_DATABASE_URL`; DSN в evidence
не записан.

| Требование | Test ID | Точная причина RED | Мутация | Ошибочный результат |
|---|---|---|---|---|
| Долговечный journal | `test_operation_run_start_and_success_are_independently_committed`, `test_operation_run_rejects_second_terminal_transition` | Нет repository | Start after action; double terminal | Running не виден или terminal переписан |
| Terminal rollback | `test_operation_terminal_sql_failure_rolls_back_to_running` | Нет repository | Partial terminal write | Несогласованные fields |
| Ready/local day/counts | `test_snapshot_uses_exact_ready_predicate_and_local_day_interval` | Нет snapshot repository | failed/uncertain as ready; UTC day; package published вместо confirmed | Ложные ready/today counts |
| Newest ten safe attempts | `test_snapshot_returns_newest_ten_delivery_attempts_with_safe_fields` | Нет snapshot repository | Newest limit reversed; reason/payload projected | Старая или чувствительная история |
| Repeatable read/read-only | `test_snapshot_remains_consistent_when_a_complete_graph_is_committed_mid_read`, `test_snapshot_is_read_only_for_every_persisted_table` | Нет snapshot repository | READ COMMITTED; status writes row | Torn snapshot или mutation при status |
| Named schema/rollback | `test_operation_runs_migration_has_exact_named_schema`, `test_operation_runs_upgrade_downgrade_reupgrade_preserves_wave_four_fixture` | Нет table/revision 05 | Missing/wrong constraint; downgrade deletes Wave 4 | Broken invariant или потеря package 1/message_id 6 |
| CLI full/partial safe report | `test_status_*` | Старый status не строит Wave 5 report | Systemd failure masks DB; DB/migration failure returns 0; sensitive stderr | Неполный/небезопасный отчёт |
| Bootstrap observation | `test_open_run_once_yields_recorded_action_and_preserves_execute_result` | Нет RecordedAction | Raw action yielded | Запуск теряется вне stdout |
| Clean wheel/head | `test_clean_wheel_contains_observability_modules_and_wave_five_head` | Нет modules/revision | Package data omission | Installed CLI не имеет Wave 5 |
