# RED-доказательства Wave 6

Дата: 2026-08-12. База Task 11: `8423107`. Граница: упаковка,
установленный UI, seeded PostgreSQL 16 и финальный gate.

## RED nodes и мутации

| Node ID | Контракт | Реальный RED | Мутация, которую ловит |
| --- | --- | --- | --- |
| `W6-R1` | Wheel содержит 6 static assets, Alembic `env.py` и head `20260812_07` | Новый e2e прошёл inspect; до него был только source-text contract | Удалить asset или migration из package data |
| `W6-R2` | Работа без checkout | Wheel собран, установлен в fresh venv; cwd в temp, subprocess env собран из allowlist | Импорт или asset lookup из checkout |
| `W6-R3` | Миграция из installed wheel | Packaged Alembic довёл изолированную схему до head | Не паковать migration tree |
| `W6-R4` | `/`, `/static/styles.css` и assets доступны | Первый e2e: `/static/styles.css` вернул `404`, тест `1 failed` | Удалить явный `/static` mount |
| `W6-R5` | Первый UI-запуск создаёт проект | После `W6-R4` `/api/v1/bootstrap` вернул `404`, тест `1 failed` | Убрать `BootstrapProject` из web composition root |
| `W6-R6` | CLI завершается без kill | Для `SIGTERM` Uvicorn дал полный shutdown log, но process code `-15`; контракт уточнён до terminal `SIGINT`/code `0` | Зависание на shutdown или forced kill |
| `W6-R7` | Browser suite не заражает event loop всего pytest | Full baseline: `16 failed, 688 passed`; minimal order browser→API: `1 failed, 73 passed`; reverse: `74 passed` | Вернуть browser fixture к `scope="session"` |
| `W6-R8` | Populated materials API сериализует immutable signals | Component RED: `503`, общий прогон `2 failed` | Вернуть `dataclasses.asdict()` на `mappingproxy` |
| `W6-R9` | Packages API читает список без status filter | Component RED: PostgreSQL ambiguous `NULL`, HTTP `503`, общий прогон `2 failed` | Вернуть `(:status IS NULL OR status=:status)` |
| `W6-R10` | Full Ruff без F401 | Первый `uv run ruff check .`: `11 errors` | Вернуть unused imports или динамический `locals()` namespace |
| `W6-R11` | Ambient app credentials не попадают в installed UI | Hostile Fernet/Telegram/HN/timezone env создал Telegram channel/route: `1 failed in 3.26s` | Вернуть `os.environ.copy()` или не передать allowlist build/install/runtime |

## GREEN по nodes

- `W6-R1`–`W6-R6`: installed-wheel e2e — `1 passed`; wheel содержит
  все assets/migrations; HTTP — `200/200/200`; bootstrap project ID `1`;
  `SIGINT` — code `0`.
- `W6-R7`: оба порядка после module-scoped fixture — `74 passed` и
  `74 passed`.
- `W6-R8`–`W6-R9`: component — `2 passed`; смежные unit —
  `12 passed`.
- `W6-R10`: scoped и full Ruff — `All checks passed!`.
- `W6-R11`: тот же hostile ambient на OS allowlist — `1 passed in 3.32s`;
  bootstrap/settings сохранили deterministic query/timezone, channel/route
  и encrypted secret в PostgreSQL отсутствуют.

Секреты и рабочие DSN в evidence не сохранялись. Все PostgreSQL
прогоны шли на отдельном PostgreSQL 16; изолированные схемы
удалялись.
